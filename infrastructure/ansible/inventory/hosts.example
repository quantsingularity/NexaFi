# Ansible Inventory Example
# Copy this file to hosts and edit with your server details
#
# Usage: ansible-playbook -i inventory/hosts playbooks/playbook.yml

[nexafi_servers]
# Production servers
# prod-server-1 ansible_host=10.0.1.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/nexafi-prod.pem
# prod-server-2 ansible_host=10.0.1.11 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/nexafi-prod.pem

[nexafi_servers:vars]
# Common variables
# ansible_python_interpreter=/usr/bin/python3
# environment=production

[staging_servers]
# Staging servers
# staging-server-1 ansible_host=10.0.2.10 ansible_user=ubuntu

[development_servers]
# Development servers
localhost ansible_connection=local
