# ----------------------------------------------------------------
# NexaFi Environment Variables Example
# ----------------------------------------------------------------

# This file contains all environment variables used by the NexaFi project.
# Copy this file to .env and fill in your desired values.

# ----------------------------------------------------------------
# 1. Core Application Configuration (Used by various backend services)
# ----------------------------------------------------------------

# Secret key for secure session management and token signing. MUST be a strong, random string.
SECRET_KEY=your_strong_random_secret_key_here

# Debug mode for the API Gateway/User Service. Set to 'True' or 'False'.
DEBUG=True

# Host and Port for the main API Gateway (e.g., User Service)
HOST=0.0.0.0
PORT=5000

# Main Database Connection URL (e.g., for User Service)
# Example for SQLite: sqlite:///./user-service.db
# Example for PostgreSQL: postgresql://user:password@host:port/dbname
DATABASE_URL=sqlite:///./user-service.db

# ----------------------------------------------------------------
# 2. Infrastructure Services Configuration (Used by shared/config/infrastructure.py)
# ----------------------------------------------------------------

# Redis Configuration (for caching, rate limiting, etc.)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# Optional: Set a password if your Redis instance requires authentication
# REDIS_PASSWORD=

# RabbitMQ Configuration (for inter-service communication/messaging)
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=nexafi
RABBITMQ_PASSWORD=nexafi123
RABBITMQ_VHOST=/

# Elasticsearch Configuration (for search and logging)
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200

# Logging and Caching
LOG_LEVEL=INFO
CACHE_DEFAULT_TIMEOUT=300
CACHE_KEY_PREFIX=nexafi:

# Rate Limiting and Circuit Breaker
RATE_LIMIT_DEFAULT=100 per hour
CIRCUIT_BREAKER_FAILURE_THRESHOLD=5
CIRCUIT_BREAKER_RECOVERY_TIMEOUT=60

# ----------------------------------------------------------------
# 3. Frontend Configuration (Used by web-frontend)
# ----------------------------------------------------------------

# API URL for the web-frontend to connect to the backend API Gateway.
# If running the backend locally on port 5000, use http://localhost:5000
VITE_API_URL=http://localhost:5000
